import Link from "next/link";
import { SearchX } from "lucide-react";
export default function NotFound(){return <main className="flex min-h-[65vh] flex-col items-center justify-center px-5 text-center"><span className="flex h-14 w-14 items-center justify-center rounded-2xl bg-muted text-muted-foreground"><SearchX className="h-7 w-7"/></span><p className="mt-5 text-sm font-semibold uppercase tracking-widest text-primary">404 — Not found</p><h1 className="mt-2 text-3xl font-bold">We couldn’t find that page</h1><p className="mt-2 max-w-md text-sm text-muted-foreground">The page may have moved or the link could be incorrect.</p><Link href="/" className="mt-5 inline-flex h-10 items-center rounded-xl bg-primary px-5 text-sm font-semibold text-primary-foreground">Back to home</Link></main>}
